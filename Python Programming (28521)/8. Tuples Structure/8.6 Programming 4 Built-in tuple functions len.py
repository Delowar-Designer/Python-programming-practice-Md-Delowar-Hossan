# Built-in tuple functions len
testTuple = ()
print(testTuple,'length is',len(testTuple))
testTuple = (1,2,3)
print(testTuple,'length is',len(testTuple))
testTuple = ("Delowar")
print(testTuple,'length is',len(testTuple))
